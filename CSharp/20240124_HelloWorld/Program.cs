using System;

// Applicatoin
// .NET Framework
// CLR(Common Language RunTime)
// VM(Virtual Machine)
// OS
// Hardwere

namespace XR {
    internal class Hell {

        // Entry Point / Entry Function
        // Class Member Function(C++)
        // Method(클래스 내부 함수)
        // param: Parameter(매개변수)
        // args: Arguments (인자)
        public static void Main(string[] _args) { //main에 매개변수를 받을 수 있음. .exe 바로가기 만들어서 속성에서 이름 뒤에 턱스트를 넣으면 들어감
            System.Console.WriteLine("Hello, World!");
            System.Console.WriteLine(_args[0]);
            System.Console.Read();
        }
    }
}